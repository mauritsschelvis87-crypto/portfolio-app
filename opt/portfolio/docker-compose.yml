services:
  portfolio:
    image: s1156856/portfolio-page:latest
    container_name: portfolio
    restart: unless-stopped
    networks:
      - proxy

networks:
  proxy:
    external: true
    name: reverse-proxy_proxy
